Laravel
  API URL : http://local.laraangular.com/

Angular
  Run this command for Bootstrap
  
  --> npm install --save bootstrap

  after run command put below code inside angular-cli.json (inside project root folder) find styles and add a bootstrap css like this:

  "styles": [
     "../node_modules/bootstrap/dist/css/bootstrap.min.css",
     "styles.css"
  ],

Angular Material
  Run this command for Angular Material
  
  --> npm install --save @angular/material @angular/animations @angular/cdk
